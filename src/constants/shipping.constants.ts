/**
 * Shipping Method Constants
 */

/** A store offers exactly one delivery option. */
export const MAX_SHIPPING_METHODS_PER_STORE = 1;

/** Shortest delivery estimate a vendor can set, in days. */
export const MIN_DELIVERY_DAYS = 1;

/** Shortest name a delivery option can be given. */
export const MIN_SHIPPING_METHOD_NAME_LENGTH = 2;

/** Shortest description a vendor must write for a delivery option. */
export const MIN_SHIPPING_DESCRIPTION_LENGTH = 25;

/** Longest description a delivery option's description can be. */
export const MAX_SHIPPING_DESCRIPTION_LENGTH = 200;
